export interface ITranslationTypes {
     subLogo: string;
     subLogoFooter: string;
     menubar: {
        delivery: string;
        price: string;
        production: string;
        payment: string;
        contacts: string;
        about: string;
    }
}